# Archive

Legacy files kept for reference only — **not used by the live app** (which is `app.py` + `backend/`).

- `Home.html` — leftover third-party "Fresh Development" template; carried an old Universal Analytics tag (`UA-6578661-52`). Nothing references it.
- `Js/` — companion scripts for `Home.html` (old `ga.js`, `contacts.js`, `cookie.js`, `inview.js`).

Safe to delete entirely if no longer needed.
